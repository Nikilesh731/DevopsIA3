apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: gateway-service
  ports:
    - port: 5000
      targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: region-service
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: region-service
  ports:
    - port: 5001
      targetPort: 5001
---
apiVersion: v1
kind: Service
metadata:
  name: simulation-service
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: simulation-service
  ports:
    - port: 5002
      targetPort: 5002
---
apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: resource-service
  ports:
    - port: 5003
      targetPort: 5003
---
apiVersion: v1
kind: Service
metadata:
  name: fault-service
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: fault-service
  ports:
    - port: 5004
      targetPort: 5004
---
apiVersion: v1
kind: Service
metadata:
  name: event-bus
  namespace: epidemic-system
spec:
  type: ClusterIP
  selector:
    app: event-bus
  ports:
    - port: 5005
      targetPort: 5005
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: epidemic-system
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080